apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "myapp.name" . }}-config
  namespace: {{ .Release.Namespace }}
  labels: {{ include "myapp.labels" . | nindent 4 }}
data:
  DB_HOST: db
  DB_NAME: visitors
  DB_USER: visitors
